import { Container, Grid } from "@mui/material";

const Login = () => {
    return (
        <Grid>
            <Container>
                login added
            </Container>
        </Grid>
    );
};

export default Login;